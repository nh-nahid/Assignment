// A Function of Checking Even / Odd Number 

function isOdd (number) {

    return (number % 2) === 0;
}
console.log(isOdd(47));

// A Function of Checking Odd / Even Number

function isEven (number){

    return (number % 2) === 0;
}
console.log(isEven(90));